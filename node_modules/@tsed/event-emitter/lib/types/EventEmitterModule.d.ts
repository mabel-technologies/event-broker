import { InjectorService, LOGGER } from "@tsed/di";
import { EventEmitterService } from "./services/EventEmitterFactory.js";
export declare class EventEmitterModule {
    protected logger: LOGGER;
    protected injector: InjectorService;
    protected eventEmitter: EventEmitterService;
    private disableSummary;
    private loadEventEmitter;
    $onInit(): void;
    $onReady(): void;
    printEvents(): void;
    private bindEventListeners;
    private getListener;
    private bindOn;
    private bindOnAny;
}
