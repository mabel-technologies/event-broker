import { Store } from "@tsed/core";
export function OnEvent(event, options) {
    return (target, propertyKey) => {
        const store = {
            onEvent: {
                [propertyKey]: { event, options }
            }
        };
        Store.from(target).merge("eventEmitter", store);
    };
}
