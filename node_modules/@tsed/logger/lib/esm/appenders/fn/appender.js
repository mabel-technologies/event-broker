import { AppendersRegistry } from "../registries/AppendersRegistry.js";
export function appender(name, target, options = {}) {
    target.$appenderOptions = {
        ...options,
        name
    };
    target.$name = name;
    AppendersRegistry.set(name, { provide: target });
}
