import { injector } from "./injector.js";
export function refValue(expression, defaultValue) {
    return Object.freeze({
        get value() {
            return injector().settings.get(expression, defaultValue);
        },
        set value(value) {
            injector().settings.set(expression, value);
        }
    });
}
