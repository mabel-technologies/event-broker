import { type Type } from "@tsed/core";
import type { InterceptorMethods } from "../interfaces/InterceptorMethods.js";
import type { TokenProvider } from "../interfaces/TokenProvider.js";
declare global {
    namespace TsED {
        interface ClassProviderBuilder<Token extends Type> {
            intercept(property: keyof InstanceType<Token>, interceptor: TokenProvider<InterceptorMethods>, options: Record<string, unknown>): this;
        }
    }
}
