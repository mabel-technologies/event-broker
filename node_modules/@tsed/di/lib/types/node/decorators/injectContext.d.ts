import { DIContext } from "../../common/domain/DIContext.js";
/**
 * Inject a context like PlatformContext or any BaseContext.
 *
 * ```typescript
 * @Injectable()
 * export class MyService {
 *   @InjectContext()
 *   ctx?: Context;
 * }
 * ```
 *
 * @returns {Function}
 * @decorator
 */
export declare function InjectContext(transform?: ($ctx: DIContext) => unknown): PropertyDecorator;
