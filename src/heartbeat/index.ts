/** Heartbeat listener. `@aws-sdk/client-cloudwatch` is an optional peer dependency: import this subpath only where it is installed. */
export { HeartbeatListener, HEARTBEAT_EVENT, HEARTBEAT_HANDLER_ID, HEARTBEAT_METRIC_NAMESPACE } from "./HeartbeatListener.js";
